pre-commit:
  parallel: true
  commands:
    fmt:
      glob: "*.rs"
      run: cargo fmt --all -- --check
    clippy:
      glob: "*.rs"
      run: cargo clippy --lib --tests -- -D warnings
    test:
      glob: "*.rs"
      run: cargo test --locked
